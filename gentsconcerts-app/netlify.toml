[build]
  command = "npm install expo@56.0.7 expo-status-bar expo-splash-screen @react-navigation/native-stack react-native-reanimated --legacy-peer-deps && npx expo export --platform web"
  publish = "dist"
