[build]
  publish = "."
  command = ""

# The SPA fallback redirect. 
# We use force = false so that existing files (including functions) are served first.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
